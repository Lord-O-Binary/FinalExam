myCourses <- c("ECON 101", "MATH 102", "ART200", "ENGLISH 203", "MATH 230", "CHEM 150", "DAEN 500", "STAT 200")
transcript_size <- length(myCourses)
first_pair <- myCourses[c(1,2)]
second_pair <- myCourses[c(3,4)]
myCourses_alpha <- sort(myCourses)
myCourses_backwards <- sort(myCourses,decreasing =TRUE)

